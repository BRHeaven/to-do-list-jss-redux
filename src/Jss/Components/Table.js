import { styled } from "styled-components";

const table = ({...resProps}) => (
    <table {...resProps}>

    </table>
);
export const Table = styled(table)`
    width: 90%;
    margin: 10px auto;
`;